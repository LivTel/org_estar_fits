# This shell script should be sourced to set environment for any Java using jfits
# Now does nothing, as modified jfits in robonet class path.
#foreach jar (${ESTAR_JAVALIB_HOME}/jfits-0.91.jar )
#    echo ${CLASSPATH} | /bin/grep "${jar}" > /dev/null
#    if ( $status != 0 ) then
#	setenv CLASSPATH ${CLASSPATH}":"${jar}
#    endif
#
#end

